

<html>

 <head>
  <meta charset="UTF-8">
  <title>Page Potin</title>
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
</head>

<body class="bg-light text-dark">

  <div class="container mt-5">

    <h1 class="mb-4 text-center text-dark">Potin !</h1>

    <div class="card mb-4">
      <div class="card-body">
        <h5 class="card-title">Potin n°<%= id %></h5>
        <p class="card-text"><strong>Auteur :</strong> <%= gossip.author %></p>
        <p class="card-text"><strong>Contenu :</strong> <%= gossip.content %></p>
        <a class="btn btn-warning me-2" href="/gossips/<%= id %>/edit/">Modifier</a>
        <a class="btn btn-secondary" href="/">Revenir en arrière</a>
      </div>
    </div>

    <div class="mb-4">
      <h4>Balance ton commentaire 💬</h4>
      <form action='/gossips/<%= id %>/newC/' method='POST' class="row g-3">
        <div class="col-md-6">
          <label class="form-label">Ton nom</label>
          <input type='text' class="form-control" name='comment_author' required>
        </div>
        <div class="col-md-6">
          <label class="form-label">Ton commentaire</label>
          <input type='text' class="form-control" name='comment_content' required>
        </div>
        <div class="col-12">
          <input type='submit' class="btn btn-success mt-2" value="Envoyer le commentaire">
        </div>
      </form>
    </div>

    <form action="/gossips/<%= id %>/deleteA/" method="POST" class="mb-4">
      <input type="submit" class="btn btn-danger" value="Supprimer tous les commentaires">
    </form>

    <div>
      <h4>Commentaires 🗨️</h4>
      <% comments.each do |comment| %>
        <div class="border rounded p-2 mb-2 bg-white">
          <strong><%= comment.author %> :</strong> <%= comment.content %>
        </div>
      <% end %>
    </div>

  </div>

</body>

  


</html>